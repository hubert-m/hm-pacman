const Settings = {
    STEP: 75,
    SIZE: 50,
    COIN_SIZE: 10, // should not be greater than STEP
    BORDER: 10 * 2,
    TOP_SCORE_BOARD_HEIGHT: 50,
    NUMBER_OF_GHOSTS: 5,
}

export default Settings;